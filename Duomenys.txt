void DuomenysInternet(Grybai & grybai)adsaddasdsa
{
 ifstream fd(u2);
// string pav, tip;
// GrybasInfo s1;
 int ns = 0;
 bool yra = true;
 while(!fd.eof() && yra) { // kol yra duomenų ir jie telpa į masyvą
 /* fd >> pav >> tip;
 dasd\d
 das
 d
 as

 sda

 cchjvkvkvkyc
 s1.Dėti (pav, tip); */
 if(!fd.eof() && (ns - 1 < Grybai::CMax ) )
 grybai[ns++] = s1; // įrašo naują elementą
 elsedasdas
 yra = false;// dasdadsasd
 dasdasd
 asdasda
 dasads
 dasad
 }
 fd.close();
 /*grybai.Dėti(ns);*/
}