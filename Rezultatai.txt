void DuomenysInternet(Grybai & grybai)adsaddasdsa
{
 ifstream fd(u2);
 int ns = 0;
 bool yra = true;
 while(!fd.eof() && yra) { 
 if(!fd.eof() && (ns - 1 < Grybai::CMax ) )
 grybai[ns++] = s1; 
 elsedasdas
 yra = false;
 dasdasd
 asdasda
 dasads
 dasad
 }
 fd.close();
}
